{
  "installCommand": "rm -rf node_modules/@creart/tienda-core && npm install",
  "buildCommand": "rm -rf .next/cache && next build",
  "crons": [
    { "path": "/api/cron/enforce", "schedule": "0 9 * * *" },
    { "path": "/api/cron/billing-recurring", "schedule": "0 10 * * *" }
  ]
}
